# Maintenance and migration

> **Placeholder** — this chapter is written under bead `acetone-4zy.5`
> (Part II: history, schema, maintenance).
>
> Planned content: routine care of a repository — `fsck`, `reindex`, garbage
> collection and consolidation, co-tenant considerations, and migrating history
> across a format-version change with `acetone migrate`.
