# Querying with Cypher: a cookbook

> **Placeholder** — this chapter is written under bead `acetone-4zy.3`
> (Part II: Querying with Cypher).
>
> Planned content: worked, verified Cypher recipes against the asset-registry
> graph — matching and filtering, traversals, aggregation, mutation
> (`CREATE`/`SET`/`DELETE`), and parameters. Every recipe is executed against
> the acetone binary while authoring; where a natural query is not yet
> supported, the gap is noted and cross-linked to the
> [conformance statement](../appendices/conformance.md).
