# The asset registry: a running example

> **Placeholder** — this chapter is written under bead `acetone-4zy.2`
> (Part I: Getting started).
>
> Planned content: the asset-registry example graph (hosts, services,
> dependencies) that the rest of the manual builds on — its schema, how to
> load it, and a tour of its shape. Later chapters (the query cookbook, the
> import walkthrough, the recovery runbook) all work against this dataset.
