# Your first graph

> **Placeholder** — this chapter is written under bead `acetone-4zy.2`
> (Part I: Getting started).
>
> Planned content: `acetone init`, declaring a schema (natural keys are
> mandatory — node identity is `(primary label, key tuple)`), creating and
> querying a few nodes and edges with Cypher, and making a first commit. Every
> example is executed against the acetone binary while authoring.
