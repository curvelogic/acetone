# cargo-deny configuration — advisories, licences, sources and duplicate bans
# run locally with: cargo deny check

[advisories]
version = 2
yanked = "deny"

[licenses]
version = 2
# Workspace crates are unpublished (publish = false) pending Greg's licence
# decision; ignore them rather than fail unlicensed.
private = { ignore = true }
# Additions require a justification in the PR per CLAUDE.md's dependency rule.
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Zlib",
    "Unicode-3.0",
]

# Greg's ruling (ADR-0003): crate-specific exception only — MPL-2.0 stays
# denied in general so any future MPL dependency forces a fresh decision.
[[licenses.exceptions]]
name = "uluru"
allow = ["MPL-2.0"]

[bans]
multiple-versions = "warn"
wildcards = "deny"
# Versionless path deps between workspace crates are not registry wildcards.
allow-wildcard-paths = true

[sources]
unknown-registry = "deny"
unknown-git = "deny"
