
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
# bd's append-only audit log: passive local export (canonical state is the
# Dolt DB synced via refs/dolt/data), written continuously by bd, never
# consumed from git — tracking it dirtied every working tree (acetone-cbl.9).
.beads/interactions.jsonl

# Rust
/target
spikes/*/target/

# mdBook build output (built in CI; see .github/workflows/docs.yml)
docs/manual/book/
